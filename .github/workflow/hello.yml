name : Hello
on:
  push :
    branches: [main]
    

jobs:
  Greet:
    runs-on : ubuntu-letest
    steps:
        - name : Say hello to user
          run : echo "Hello Dosto"
        - name : say hello to username
          run : echo "Hello $USER"
#this is a example of how runers and jobs are running and working