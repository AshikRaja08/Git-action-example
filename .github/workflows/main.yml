---
name: Hello
on:
  push:
    branches:
      - main
jobs:
  Greet:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello to user
        run: echo "Hello Dosto"
      - name: say hello to username
        run: echo "Hello $USER"
        
        
 details:
  runs-on: ubuntu-latest
  steps:
    - name: show version of python,docker
      run: python --version && docker --version
